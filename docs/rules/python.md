# Python Rules

- Always use a virtualenv (`source .venv/bin/activate`).
- Type hint all function signatures.
- Adhere to PEP 8 standards and use `black` for formatting.
- Prefer Python 3.10+ features like structural pattern matching where appropriate.
