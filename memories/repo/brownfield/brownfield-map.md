# Brownfield Archaeology Map

> Generated by starter-init Phase A sweep. Date: 2026-06-28

## Repository Identity

- **Name**: chatbot-with-data
- **Type**: Production-grade RAG system (portfolio/learning project)
- **Language**: Python 3.10+ (FastAPI backend) + JavaScript (React/Vite frontend)
- **Database**: SQLite (metadata) + Weaviate 1.27.0 (vector DB via Docker)
- **LLM**: OpenAI GPT-4o / compatible endpoint

## Baseline Commands

| Command | Purpose | Status |
|---------|---------|--------|
| `uvicorn main:app --reload --host 0.0.0.0 --port 8000` (from `backend/`) | Backend server | No tests available |
| `npm run dev` (from `frontend/`) | Frontend dev server | No tests available |
| `npm run build` (from `frontend/`) | Frontend build | Available |
| `pytest` | Backend tests | No test files exist |
| `npm test` | Frontend tests | No test files exist |
| `docker-compose up -d` | Start Weaviate | Available |

**Note**: No test files exist for either backend or frontend. Evaluation harness (`POST /chat/evaluate/sanity-check`) exists at runtime but no golden dataset found.

## Security-Sensitive Paths

| Path | Risk | Reason |
|------|------|--------|
| `backend/config.py` | HIGH | Loads API keys from `.env` (dotenv) |
| `backend/.env` | CRITICAL | Committed secrets — contains `OPENAI_API_KEY` |
| `backend/.env.local` | CRITICAL | Committed secrets — duplicate of `.env` |
| `backend/chat/safety.py` | HIGH | 3-layer prompt-injection defense (Heuristic + Fuzzy + LLM scanner) |
| `backend/config/injection_patterns.yaml` | MEDIUM | 49 regex patterns for injection detection |
| `backend/config/injection_corpus.json` | MEDIUM | Known injection corpus for fuzzy matching |
| `backend/providers/factory.py` | HIGH | Provider factory — propagates API keys to LLM/embedding providers |
| `backend/providers/openai.py` | HIGH | Makes external API calls with secrets |
| `backend/indexing/weaviate_store.py` | MEDIUM | Vector DB client with configurable credentials |
| `backend/extractors/web_extractor.py` | MEDIUM | URL fetching (potential SSRF vector) |
| `backend/routers/*.py` | MEDIUM | All 7 API routers — no auth middleware |
| `backend/chat/retrieval.py` | MEDIUM | RAG retrieval pipeline |
| `backend/chat/generation.py` | MEDIUM | LLM answer generation |
| `backend/chat/streaming.py` | MEDIUM | SSE streaming (no output moderation) |

## Preserved Behavior Baseline

1. **Prompt injection defense must remain active**: The 3-layer safety check (heuristic → fuzzy → LLM) in `backend/chat/safety.py` must always run before any user query reaches the retrieval or generation pipeline.
2. **Hybrid search (BM25 + vector) must remain the default retrieval strategy**: The `CandidateMerger` with RRF fusion in `backend/chat/retrieval.py` is the core differentiator.
3. **SQLite schema must be backward-compatible**: 16 tables across 4 migration versions exist. Any schema changes must not break the existing migration chain in `backend/migrations/runner.py`.

## Profile Rules

Any feature touching a path rated `HIGH` or `CRITICAL` MUST be promoted to `Complex` in its `status.md`.

Paths affected: `backend/config.py`, `backend/.env`, `backend/.env.local`, `backend/chat/safety.py`, `backend/providers/factory.py`, `backend/providers/openai.py`.

## Existing Entrypoints

- `AGENTS.md` — Router-style, GitNexus-focused. No conflicts with harness kit.
- `CLAUDE.md` — Duplicate of `AGENTS.md`. May need consolidation.

## Test Inventory

- Backend: pytest available, but zero test files exist.
- Frontend: vitest configured with jsdom, but `src/__tests__/` directory does not exist.
- CI: No CI pipeline configured (no `.github/`, `.gitlab-ci.yml`, or similar).
